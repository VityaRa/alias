export enum SentMessages {
  LOGIN = 'user:login',
  JOIN = 'user:join',
}

export enum IncomingMessages {
  GET = 'user:get',
  JOINED = 'user:joined',
  CONNECTED = 'connected',
  DISCONNECTED = 'disconnected',
}